import styled from 'styled-components'

import {
  background,
  margin,
  padding,
  display,
  flexbox,
} from '~/components/Theme/styled'

export const Box = styled('div')`
  ${background}
  ${padding}
  ${margin}
  ${display}
  ${flexbox}
`
